<UserControl x:Class="barleyBreak.View.MenuPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:barleyBreak.View"
             xmlns:uiControll="clr-namespace:barleyBreak.View.UIControll"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" x:Name="btnStart" Style="{StaticResource MenuButton}" Content="New Game" Click="BtnStart_OnClick"></Button>
        <Button Grid.Row="1" x:Name="btnSettings" Style="{StaticResource MenuButton}" Content="Settings" Click="btnSettings_Click"></Button>
        <Button Grid.Row="2" x:Name="btnExit" Style="{StaticResource MenuButton}" Content="Exit" Click="btnExit_Click"  ></Button>
    </Grid>
</UserControl>
